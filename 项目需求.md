# BMS老化测试上位机 - 项目需求文档

## 1. 项目概述
本上位机软件主要用于 BMS（电池管理系统）的老化测试。软件需支持多通道并发测试，并具备灵活的“主-从”拓扑配置、扫码绑定入站、用例工步编辑以及单通道独立调试等核心功能。

## 2. 核心功能需求

### 2.1 BMS 拓扑与通道管理
- **主从模式配置**：每个测试通道支持“1主多从”的模式，且必须支持灵活配置：
  - 1主0从
  - 1主1从
  - 1主2从
  - 1主3从
- **通道数量与扩展**：当前系统标准配置为 **48 通道**，软件架构设计上必须具备无缝扩展至 **60 通道** 的能力。

### 2.2 扫码绑定与语音提示工作流
在测试正式开始前，必须经过扫码流程：
- **货架码静态映射配置**：系统需在后台预先配置好【货架码】与【测试通道（1~60）】的一一匹配关系。
- **扫描货架码定位通道**：操作员首先扫描货架码，系统通过配置表自动匹配，并锁定将要入站的那个特定测试通道。
- **扫描设备码**：通道被定位锁定后，继续扫描该通道内配置的主机编码，以及相应数量的从机编码，完成该通道的全部扫码入站。
- **语音提示联动**：在弹出扫码框以及扫码操作期间，软件需连接并控制外部“语音模块”，给出相应的操作语音提示（语音模块通讯协议待后续提供）。

### 2.3 测试用例与工步管理
- **工步编辑器**：软件需具备完善的测试项目和测试工步的编辑功能，允许用户定义测试的各个阶段。
- **用例复用机制**：测试项目应设计为可复用的副本。用户只需编写好一次测试用例，即可下发并应用到所有通道上，极大地减少重复配置工作。

### 2.4 调试与设备管理
- **单通道/设备独立调试**：软件需提供专门的调试功能界面，便于工程师脱离主测试流程，单独对某个通道或某个特定设备（如指定的BMS或测试机）进行硬件校验、通信报文收发测试及手动参数下发。
- **全局设备配置**：需具备一个全局的硬件配置中心（界面），专门用于设置系统涉及的所有外设参数（如扫码枪、语音模块COM口、所有测试仪器的IP或波特率等）。

### 2.5 数据记录与存储
- **全通道数据追踪**：测试运行期间，48~60 个通道的测试记录必须独立且实时保存。
- **双轨存储机制**：
  - **本地数据库**：建立本地数据库（如 SQLite）对各个测试工步数据进行结构化存储，便于日后条件查询。
  - **本地 XTML 文本**：每次测试结束后，生成对应通道的本地 XTML 格式文本文件，作为原始数据备份或外部系统导入使用。

### 2.6 异常判定与 NG 处理机制
- **灵活的 NG 判定策略**：在用例或工步编辑时，需具备对异常测试结果（NG）的处理设置功能，支持以下三种执行策略：
  1. **遇到任何 NG 立即停止**：本通道内只要出现任何测试项不合格，立即终止该通道后续所有测试，并抛出异常提示。
  2. **遇到指定 NG 停止**：允许用户自定义关键测试项。仅当这些特定的“致命项”发生 NG 时终止测试；如果是非致命项 NG，则仅作记录并继续测试。
  3. **遇到 NG 继续测试**：不论哪个项目发生 NG，均仅作记录（如标红），测试流程不会中断，直到所有工步强制执行完毕，最后统一出具包含 NG 项的报告。

## 3. 界面 (UI) 需求
- **整体规划**（初步建议）：
  - **多通道总览界面**：使用网格或卡片形式实时展示 48~60 个通道的状态（空闲、测试中、异常、通过等）。
  - **扫码入站弹窗**：操作员联动语音的扫码界面，包含当前扫码进度提示。
  - **工步/用例配置界面**：类似于组态软件的步骤添加、NG策略配置及参数调整界面。
  - **系统硬件配置界面**：统一管理所有连接设备通讯参数和本地环境配置。
  - **单通道/单设备调试界面**：针对单通道和单设备，提供底层的指令下发、强制动作和报文监控界面。

## 4. 技术栈要求
- **核心框架**：Python + PyQt (如 PyQt5 / PyQt6 或 PySide6)
- **UI 界面**：Qt Designer (推荐) 或 QML
- **通讯类库**：需准备与语音模块、BMS、测试设备通讯的相关库（串口 `pyserial` 或网络 `socket` 等）。

## 6. 关键设备协议记录

### 6.1 万瑞达/允星河 RU 系列电源 (AFE & 主机电源)
系统集成了两台 RU 系列直流电源，通讯协议均遵循《万瑞达电气 RU 电源 LAN 通讯协议-V1.4》。

- **物理连接**：以太网 TCP（默认端口 2000）。
- **通讯协议**：Modbus TCP (标准 SOCKET 帧结构，不带 CRC 校验)。
- **Slave ID**：默认 1。

#### 寄存器地址定义 (十进制)
- **读取电压**：`100` (Input Register, 功能码 04)
- **读取电流**：`101` (Input Register, 功能码 04)
- **设定电压**：`149` (Holding Register, 功能码 06)
- **设定电流**：`150` (Holding Register, 功能码 06)
- **输出开关**：`133` (Coil, 功能码 05, 开启: 0xFF00, 关闭: 0x0000)

#### 实测倍率记录 (重要)
由于硬件固件版本差异，不同型号的内部逻辑倍率有所不同，必须在驱动层独立处理：
1. **1# AFE 供电电源 (RU36-100V36A)**:
   - **电压**：10 倍率 (0.1V 步进)。
   - **电流**：10 倍率 (0.1A 步进)。
2. **主机供电电源 (RU60-30V200A)**:
   - **电压**：100 倍率 (0.01V 步进)。
   - **电流**：10 倍率 (0.1A 步进)。

#### 调试记录
- **地址误区**：手册标注的 `0x64/0x95` 等为十六进制地址，在 Python `pymodbus` 中必须转换为十进制 `100/149` 使用。LabVIEW 示例中的 `64/95` 为十六进制显示。
- **稳定性**：必须在读取循环中加入互斥锁及超时判断，防止在网络延迟或物理断开时产生线程积压导致界面“未响应”。
